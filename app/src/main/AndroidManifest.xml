<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fhit.recyclerviewdemo">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".TApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.linerLayoutManager.LinerLayoutManagerVerticalActivity" />
        <activity android:name=".ui.linerLayoutManager.LinerLayoutManagerHorizontalActivity" />
        <activity android:name=".ui.gridLayoutManager.GridLayoutActivity" />
        <activity android:name=".ui.staggeredGridLayoutManager.StaggeredGridLayoutManagerActivity" />
        <activity android:name=".ui.refreshLayout.RefreshRecyclerViewActivity" />
        <activity android:name=".ui.refreshLayout.SwipeRecyclerViewActivity" />
        <activity android:name=".ui.gridLayoutManager.IrregularGridLayoutActivity"></activity>
    </application>

</manifest>